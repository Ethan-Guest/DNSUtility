namespace DNSUtility.Ui;

public class AutoMapperConfig
{
    public void Configure()
    {
        /*var config = new MapperConfiguration(cfg => cfg.CreateMap<, Nameserver>());
        config.CreateMapper();*/
    }
}