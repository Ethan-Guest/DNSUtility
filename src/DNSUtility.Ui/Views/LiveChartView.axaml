<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lvc="using:LiveChartsCore.SkiaSharpView.Avalonia"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="DNSUtility.Ui.Views.LiveChartView">

    <Border Background="#2A2A41">
        <Grid ShowGridLines="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="20" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <!--
            <Ellipse Grid.Column="1" HorizontalAlignment="Left" Width="7" Height="7" Fill="{Binding _mainViewModel.NameserverListViewModel.SelectedNameserver.StatusIcon}" />
            -->
            <lvc:CartesianChart Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Series="{Binding Series}"
                                TooltipFindingStrategy="CompareAll"
                                DrawMarginFrame="{Binding DrawMarginFrame}" XAxes="{Binding XAxes}"
                                YAxes="{Binding YAxes}" />
        </Grid>
    </Border>

</UserControl>